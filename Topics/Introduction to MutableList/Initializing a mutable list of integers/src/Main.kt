fun main() {
    val numbers = mutableListOf(12, 17, 8, 101, 33)

    for (i in 0..4) {
        var num = numbers[i]
//        print("$num, ")
    }

    // do not touch the lines below 
    println(numbers.joinToString())
}