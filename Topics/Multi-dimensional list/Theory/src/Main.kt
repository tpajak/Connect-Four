// You can experiment here, it won’t be checked

fun main(args: Array<String>) {
  // put your code here

    val inputList = mutableListOf(
        mutableListOf<Int>(1, 0, 2),
        mutableListOf<Int>(0, 0, 0),
        mutableListOf<Int>(3, 0, 4)
    )

            println("${inputList[0][0]}+${inputList[0][2]}")
            println("${inputList[2][0]}+${inputList[2][2]}")
            println("=".repeat(10))

    for (i in 0..inputList[0].size) {
        for (j in array) {
            print("${j[0][0]}+${j[0][j.si]}")
        }
        println("${array.first()} ${array.last()}")
    }
}
